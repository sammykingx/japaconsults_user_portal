// jshint esversion:6
import { emptySplitApi } from "../api";

export const adminAPI = emptySplitApi.injectEndpoints({
    endpoints: () => ({
        // Define your admin endpoints here

    })
})

export const { } = adminAPI